{
  "version": 2,
  "buildCommand": "pnpm --filter @workspace/zynth run build",
  "outputDirectory": "artifacts/zynth/dist",
  "installCommand": "pnpm install --ignore-scripts",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
