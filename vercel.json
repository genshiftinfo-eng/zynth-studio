{
  "version": 2,
  "buildCommand": "pnpm --filter @workspace/zynth run build && mkdir -p public && cp -r artifacts/zynth/dist/public/. public/",
  "outputDirectory": "public",
  "installCommand": "pnpm install --ignore-scripts",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
